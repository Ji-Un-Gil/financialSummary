# 2026-09-18 예약 발행 중단 조사

## 확인된 원인

ChatGPT 대화 `6aa7e4d3-a1cc-83e9-86b1-046d26e10778`의 9월 18일 실행 결과는 ‘발행 보류 — 저장하지 않았습니다’였다. 선행 지침·양식·발행 목록을 GitHub 및 raw 경로에서 읽으려다 웹 접근 계층의 Cache miss / Failed to fetch로 중단했다. save_daily_summary 호출 전 단계였으므로 이 기록으로 GitHub 쓰기 권한이나 토큰 만료가 원인이라고 판단하지 않는다.

조사 시작 실제 시각은 2026-09-18T13:39:14Z였다. 조사 환경의 웹 도구에서도 raw의 docs/editorial-policy.md 접근이 Cache miss로 실패했다. git fetch는 성공했고 원격 HEAD는 블로그 배포 커밋 8f03760이었다. 9월 18일 보고서가 저장되지 않은 상태를 확인했다.

## 수정 방식

필수 지침을 웹에서만 읽어야 시작할 수 있었던 단일 의존성을 제거한다. 저장소에서 실제 읽은 AGENTS.md, 편집 원칙, 실행 절차, 보고서·검증표 양식 전체를 예약 프롬프트 안에 포함한다. 파일별 SHA256과 기준 Git 커밋을 기록한다. 최신 원격 지침 확인은 계속 시도하며 충돌을 발견하면 중단한다. 원격을 읽지 못한 경우 내장 지침 사용을 검증표에 명시한다.

뉴스 원문 확인, 불확실한 주장 제외, 직접 작성한 표·도식, 발행 전 검토는 유지한다. 발행 목록 실패와 뉴스 원문 실패를 구분한다. 목록을 읽지 못한 경우 확인 가능한 실행 기록 또는 명시적인 24시간 범위를 사용하고 누락 복구의 한계를 기록한다. 기존 파일 덮어쓰기는 Writer가 계속 거절한다.

## 유지보수

정책·양식을 바꿀 때 `python3 scripts/build-cloud-prompt.py`로 `docs/cloud-schedule-prompt.txt`를 생성하고 기존 ChatGPT 일일 예약 지시를 그 내용으로 갱신한다. 생성 스크립트만 실행했다고 원격 예약이 바뀌지는 않는다. 원격과 스냅샷은 자동 동기화되지 않으므로 갱신 후 예약 편집 화면에서 확인한다. 새 중복 예약은 만들지 않는다.

## 검증 기록

- 내장된 다섯 문서 전체 텍스트와 SHA256이 작업 저장소 원문과 일치함을 검사했다.
- 기존 예약 ID `6aa7e4f9b49c8191b599c995bdca5f93`를 수정했다. 편집 화면에서 내장 커밋 버전과 마지막 양식까지 포함한 7,355자 지침이 저장된 것을 확인했다(파일 끝 개행 제외). 매일 18:30 Asia/Seoul 유지, 다음 정기 실행 9월 19일.
- 9월 18일분을 같은 ChatGPT 대화에서 수정된 지침으로 실제 재실행했다. 실제 저장 커밋은 `92a774afd6ca5c00166a0919427982ae052ad6ce`, 서버 반환 시각은 `2026-09-18T14:13:34.324Z`, saved=true였다. git fetch로 보고서·검증표·목록 3개 파일의 원격 생성을 확인했다. 이는 수정 후 수동으로 시작한 클라우드 복구 실행이며 다음 정시 무인 실행 성공과 구분한다.

- 복구 보고서 검토에서 공표 예정 시각을 실제 공개 시각으로 단정한 부분을 발견했다. 원문 HTML과 12:03 기사 본문을 추가 대조하고 정정 기록을 남겼다. 숫자는 일치해 유지했다. 이 구분과 영구 상세 링크 우선 규칙도 예약에 추가했다.
- 최종 지침 스냅샷의 기준 커밋은 92a774a이다. 다섯 원문 파일의 해시는 이전 스냅샷과 동일하다. 지침을 내장했다고 뉴스 내용의 사실성이 자동 보증되는 것은 아니다.
